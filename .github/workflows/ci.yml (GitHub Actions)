name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Hardhat tests
      run: |
        cd contracts
        npm ci
        npx hardhat test
